# Zal MVP — Слоты для командных видов спорта

> Сервис для игроков без команды, который помогает быстро найти и занять место в игре в ближайшем спортзале с прозрачными правилами и предсказуемой стоимостью.

---

## Содержание

1. [Обзор проекта](#1-обзор-проекта)
2. [Объем MVP](#2-объем-mvp)
3. [Архитектура](#3-архитектура)
4. [Как запустить локально (Docker Compose)](#4-как-запустить-локально-docker-compose)
5. [Как запускать миграции](#5-как-запускать-миграции)
6. [Как заполнить демо-данные](#6-как-заполнить-демо-данные)
7. [Сводка API эндпоинтов](#7-сводка-api-эндпоинтов)
8. [Процесс внесения изменений](#8-процесс-внесения-изменений)
9. [Процесс работы с issue и доской](#9-процесс-работы-с-issue-и-доской)

---

## 1. Обзор проекта

**Zal** помогает игрокам без команды находить свободные игровые слоты в местных залах, присоединяться к ним и оплачивать участие — прямо с телефона.

Какие проблемы решает сервис:
- Сложно найти любительские игры в ближайших залах
- Непрозрачная стоимость и правила отмены
- Неявки и отмены в последний момент срывают игры

Пользователь открывает PWA на телефоне, выбирает вид спорта, район и время, затем просматривает доступные слоты, читает правила и бронирует место в один тап.

---

## 2. Объем MVP

### ✅ Обязательно
- Каталог слотов с фильтрами (вид спорта, район, диапазон дат)
- Страница деталей слота («карточка слота») с правилами, ценой и политикой отмены
- Присоединение/бронирование места (с защитой от гонок и овербукинга)
- Имитация оплаты (переводит статус участия в `PAID`)
- Страница «Мои участия»
- Admin API для создания слотов + наполнение демо-данными

### 🟡 Желательно
- Базовые уведомления внутри приложения
- Admin веб-страница для создания слотов

### ❌ Вне рамок MVP
- Реальный платежный провайдер (Stripe и др.)
- Push-уведомления
- Микросервисы / Kafka / gRPC
- Нативные iOS / Android приложения (только PWA)

---

## 3. Архитектура

Диаграммы переведены в Mermaid и вынесены в [docs/mermaid.md](docs/mermaid.md).

Подробности по архитектуре: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).

---

## 4. Как запустить локально (Docker Compose)

**Требования:** [Docker Desktop](https://www.docker.com/products/docker-desktop/) (или Docker + Docker Compose v2).

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/<your-org>/zal-mvp.git
cd zal-mvp

# 2. Скопируйте переменные окружения
cp .env.example .env

# 3. Запустите всё (DB + API + Web)
docker compose up --build
```

Сервисы будут доступны по адресам:

| Сервис | URL |
|---------|-----|
| Web PWA | http://localhost:5173 |
| API     | http://localhost:8080 |
| DB      | postgresql://postgres:postgres@localhost:5432/zal |

---

Остановить: `docker compose down`  
Сбросить БД: `docker compose down -v`

---

## 5. Как запускать миграции

Миграции запускаются автоматически при старте API через `golang-migrate`.

Для ручного запуска:

```bash
# Внутри запущенного контейнера api
docker compose exec api ./migrate -path /app/migrations -database "$DATABASE_URL" up

# Или через локально установленный migrate CLI
migrate -path apps/api/migrations -database "postgresql://postgres:postgres@localhost:5432/zal?sslmode=disable" up
```

Файлы миграций находятся в `apps/api/migrations/`.

---

## 6. Как заполнить демо-данные

```bash
# Заполнить через admin API (нужен ADMIN_TOKEN из .env)
curl -X POST http://localhost:8080/admin/slots \
  -H "Content-Type: application/json" \
  -H "X-Admin-Token: secret" \
  -d '{
    "sport": "football",
    "district": "Центральный",
    "venue_name": "Стадион Динамо",
    "address": "ул. Ленина 1",
    "starts_at": "2026-04-01T18:00:00Z",
    "duration_minutes": 60,
    "capacity": 10,
    "min_players": 6,
    "deadline_at": "2026-04-01T16:00:00Z",
    "expected_price": 500,
    "max_price": 700,
    "rules_text": "Casual game, no tackles."
  }'
```

Скрипт наполнения (для CI / локального демо) находится в `apps/api/cmd/seed/main.go`.

---

## 7. Сводка API эндпоинтов

| Метод | Path | Auth | Описание |
|--------|------|------|-------------|
| GET | `/health` | — | Проверка доступности |
| GET | `/slots` | — | Список слотов (`?sport=&district=&date_from=&date_to=`) |
| GET | `/slots/{slotId}` | — | Детали слота |
| POST | `/slots/{slotId}/join` | User token | Забронировать место (409 если мест нет) |
| POST | `/slots/{slotId}/pay` | User token | Имитация оплаты → PAID |
| GET | `/me/participations` | User token | Мои бронирования |
| POST | `/admin/slots` | Admin token | Создать слот |

Полная спецификация API: [docs/API.md](docs/API.md)

---

## 8. Процесс внесения изменений

Мы используем **trunk-based development** с короткоживущими ветками.

```
main  ←── feature/<ticket>-short-desc  (PR + review)
      ←── fix/<ticket>-short-desc
      ←── tech/<ticket>-short-desc
```

### Шаги

1. Возьмите issue из колонки **Ready** на project board.
2. Создайте ветку: `git checkout -b feature/42-slot-filters`
3. Внесите изменения, коммитьте чаще с понятными сообщениями:  
   `git commit -m "feat(slots): add district filter"`
4. Запушьте ветку и откройте Pull Request по [шаблону PR](.github/pull_request_template.md).
5. Запросите ревью минимум у одного участника команды.
6. После одобрения объедините в `main` (предпочтительно squash merge).

### Соглашение по commit messages

```
<type>(<scope>): <short description>

Типы: feat, fix, refactor, test, docs, chore
Области: slots, participation, payments, admin, web, db, infra
```

---

## 9. Процесс работы с issue и доской

Используем GitHub Issues + GitHub Project board с такими колонками:

| Колонка | Значение |
|--------|---------|
| **Backlog** | Идеи и будущие задачи — без приоритизации |
| **Ready** | Задачи с понятным объемом, готовые к работе |
| **In Progress** | Кто-то активно выполняет задачу |
| **In Review** | PR открыт и ожидает ревью |
| **Done** | Изменения влиты в main |

### Для нетехнических участников команды

- **Сообщить о баге:** создайте issue → выберите шаблон _Bug Report_.
- **Запросить фичу:** создайте issue → выберите шаблон _Feature Request_.
- **Отследить техническую задачу:** используйте шаблон _Tech Task_.
- Назначьте labels (см. ниже) и переместите карточку в нужную колонку.

### Labels

| Label | Значение |
|-------|---------|
| `type:feature` | Новая фича или пользовательская история |
| `type:bug` | Что-то сломано |
| `type:tech` | Рефакторинг, инфраструктура, инструменты |
| `type:docs` | Документация |
| `priority:must` | Блокер для MVP |
| `priority:should` | Важно, но не блокирует |
| `area:web` | Фронтенд |
| `area:api` | Бэкенд |
| `area:db` | База данных / миграции |
| `good first issue` | Подходит новичкам |

### Что сейчас нужно завести в Issues (полные заготовки по шаблонам)

Ниже — **готовые тексты issue** в формате текущих шаблонов из `.github/ISSUE_TEMPLATE/`.  
Скопируйте блок, создайте issue нужного типа и вставьте содержимое в поля формы.

#### 1) Feature: Каталог слотов с фильтрами

- Title: `[Feature] Каталог слотов с фильтрами`
- Labels: `type:feature`, `priority:must`, `area:web`, `area:api`
- Problem / User Story:  
  `Как игрок без команды, я хочу видеть каталог доступных слотов с фильтрами по виду спорта, району и диапазону дат, чтобы быстро находить подходящие игры рядом и в нужное время.`
- Proposed Solution:  
  `Добавить экран/страницу каталога слотов с фильтрами: sport, district, date_from, date_to. На фронтенде — форма фильтров и список карточек. На бэкенде — поддержка фильтрации в GET /slots и корректная валидация параметров.`
- Acceptance Criteria:
  - [ ] Пользователь может отфильтровать слоты по виду спорта
  - [ ] Пользователь может отфильтровать слоты по району
  - [ ] Пользователь может задать диапазон дат
  - [ ] API `/slots` возвращает отфильтрованный список по query-параметрам
  - [ ] Пустой результат корректно отображается в UI
- Area: `area:web`
- Priority: `priority:must`

#### 2) Feature: Страница деталей слота

- Title: `[Feature] Страница деталей слота (правила, цена, отмена)`
- Labels: `type:feature`, `priority:must`, `area:web`, `area:api`
- Problem / User Story:  
  `Как игрок, я хочу открыть карточку конкретного слота и увидеть правила, стоимость и политику отмены, чтобы принять решение о бронировании.`
- Proposed Solution:  
  `Реализовать страницу деталей слота на основе GET /slots/{slotId}. Отображать название площадки, время, длительность, правила, expected/max price, дедлайн и ключевые ограничения.`
- Acceptance Criteria:
  - [ ] По клику на слот открывается страница деталей
  - [ ] На странице отображаются правила, expected_price и max_price
  - [ ] Отображается политика/дедлайн отмены в понятном виде
  - [ ] При несуществующем slotId показывается корректная ошибка
  - [ ] API `/slots/{slotId}` отдает все поля, нужные UI
- Area: `area:web`
- Priority: `priority:must`

#### 3) Feature: Присоединение к слоту с защитой от овербукинга

- Title: `[Feature] Присоединение к слоту с защитой от овербукинга`
- Labels: `type:feature`, `priority:must`, `area:api`
- Problem / User Story:  
  `Как игрок, я хочу занимать место в слоте одним действием, чтобы гарантированно попасть в игру, если есть свободные места.`
- Proposed Solution:  
  `Реализовать/доработать POST /slots/{slotId}/join с атомарной проверкой доступной емкости и защитой от гонок. При отсутствии мест возвращать 409, при успехе — создавать участие со статусом RESERVED.`
- Acceptance Criteria:
  - [ ] При наличии мест запрос join завершается успешно
  - [ ] При полном слоте API возвращает 409
  - [ ] Параллельные запросы не приводят к овербукингу
  - [ ] Созданное участие связано с пользователем и слотом
  - [ ] Ошибки валидации/авторизации обрабатываются корректно
- Area: `area:api`
- Priority: `priority:must`

#### 4) Feature: Имитация оплаты (PAID)

- Title: `[Feature] Имитация оплаты (перевод участия в PAID)`
- Labels: `type:feature`, `priority:must`, `area:api`
- Problem / User Story:  
  `Как игрок, я хочу подтвердить участие через имитацию оплаты, чтобы мой статус в слоте стал оплаченным.`
- Proposed Solution:  
  `Реализовать/доработать POST /slots/{slotId}/pay: для участия текущего пользователя переводить статус RESERVED -> PAID, предотвращать повторную оплату и обрабатывать невалидные состояния.`
- Acceptance Criteria:
  - [ ] Оплата переводит статус участия в PAID
  - [ ] Повторная оплата одного и того же участия блокируется
  - [ ] Нельзя оплатить, если пользователь не присоединялся к слоту
  - [ ] Возвращаются понятные коды/тексты ошибок
  - [ ] Изменение статуса отражается в `/me/participations`
- Area: `area:api`
- Priority: `priority:must`

#### 5) Feature: Страница «Мои участия»

- Title: `[Feature] Страница «Мои участия»`
- Labels: `type:feature`, `priority:must`, `area:web`, `area:api`
- Problem / User Story:  
  `Как игрок, я хочу видеть список своих бронирований и их статус, чтобы понимать, куда я записан и что уже оплачено.`
- Proposed Solution:  
  `Добавить страницу «Мои участия», которая получает данные из GET /me/participations и показывает список участий с ключевыми данными слота и статусами RESERVED/PAID.`
- Acceptance Criteria:
  - [ ] Страница доступна авторизованному пользователю
  - [ ] Список содержит дату/время, площадку, вид спорта и статус
  - [ ] Статусы участия отображаются явно (RESERVED/PAID)
  - [ ] При отсутствии данных показывается пустое состояние
  - [ ] Ошибки API отображаются в понятном виде
- Area: `area:web`
- Priority: `priority:must`

#### 6) Tech: Admin API + seed демо-данных

- Title: `[Tech] Admin API для создания слотов + наполнение демо-данными`
- Labels: `type:tech`, `priority:must`, `area:api`, `area:db`
- Context / Motivation:  
  `Для проверки пользовательских сценариев нужен быстрый способ создавать слоты и наполнять систему тестовыми данными. Без этого сложно демонстрировать и тестировать MVP.`
- Task Description:  
  `Реализовать/доработать защищенный POST /admin/slots (по X-Admin-Token), валидацию входных данных и seed-скрипт для создания набора демонстрационных слотов в разных районах/видах спорта/времени.`
- Definition of Done:
  - [ ] POST `/admin/slots` работает с `X-Admin-Token`
  - [ ] Входные поля валидируются и ошибки возвращаются корректно
  - [ ] Можно создать слот через curl из README
  - [ ] Seed-скрипт создает предсказуемый демо-набор слотов
  - [ ] Данные seed пригодны для ручного тестирования ключевых флоу
- Area: `area:api`
- Priority: `priority:must`

#### 7) Feature: Базовые уведомления внутри приложения

- Title: `[Feature] Базовые уведомления внутри приложения`
- Labels: `type:feature`, `priority:should`, `area:web`, `area:api`
- Problem / User Story:  
  `Как игрок, я хочу видеть уведомления в интерфейсе о результате действий (успех/ошибка бронирования, оплаты), чтобы понимать текущий статус без перезагрузки.`
- Proposed Solution:  
  `Добавить базовый механизм in-app уведомлений (toast/alert) на ключевые действия: join/pay, загрузка списка, ошибки API. Стандартизировать тексты и уровни уведомлений.`
- Acceptance Criteria:
  - [ ] После успешного join показывается уведомление об успехе
  - [ ] После успешной оплаты показывается уведомление об успехе
  - [ ] Ошибки API показываются как уведомления об ошибке
  - [ ] Уведомления не блокируют основной сценарий
  - [ ] Тексты уведомлений понятны и единообразны
- Area: `area:web`
- Priority: `priority:should`

#### 8) Feature: Admin web-страница создания слотов

- Title: `[Feature] Admin web-страница для создания слотов`
- Labels: `type:feature`, `priority:should`, `area:web`
- Problem / User Story:  
  `Как администратор, я хочу создавать слоты через веб-форму, чтобы не использовать curl для каждой операции.`
- Proposed Solution:  
  `Добавить простую admin-страницу с формой создания слота и отправкой данных в POST /admin/slots с admin-токеном. Предусмотреть валидацию формы и отображение результата.`
- Acceptance Criteria:
  - [ ] Есть веб-форма с обязательными полями слота
  - [ ] Форма валидирует базовые ошибки до отправки
  - [ ] При успешной отправке слот создается через Admin API
  - [ ] Ошибки авторизации и валидации отображаются в UI
  - [ ] Страница пригодна для ручного наполнения демо-данными
- Area: `area:web`
- Priority: `priority:should`
